# Website source

Read README.md and site.json first. Current public content lives in src/pages.py. Build with python3 build.py. Keep the four design CSS files consistent with the companion project when both are available. Source packages are independent; never add sibling-folder imports or required external asset links.

The public output directory is dist. docs and handoff files are internal package material. Preserve the existing .openai/hosting.json Site identity in this workspace. Old research scripts or legacy source files, if present in history, are not the public content authority for this edition.
