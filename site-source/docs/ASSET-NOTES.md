# Asset notes

`src/assets.json` lists original names, sources, dimensions, and SHA-256 hashes. Every referenced image is included locally. Image masters have not been altered. Only used assets are copied into generated output; the source may retain additional selected archive assets.

User-supplied archive images document Keir’s career. The three advisor scenes are previously generated fictional editorial illustrations; they do not depict actual clients, events, or the agency’s staff. The visual style follows the approved guide. No new photos or synthetic likenesses were generated in this edition.

Fonts are embedded in fonts.css, carried forward from the approved design files. CSS supplies the paper texture, brushed metal, and walnut treatments. No external font service, image host, or stylesheet is required for rendering.

Source image provenance for the generated scenes is included in IMAGE-GENERATION-PROVENANCE.json. Keep the original master assets if producing responsive/optimized versions during production integration.
